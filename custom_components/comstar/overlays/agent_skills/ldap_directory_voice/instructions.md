# LDAP directory (voice)

- Tools: `lookup_user`, `list_comstar_users` on MCP `ldap_directory`.
- Call a tool before naming people or saying someone is unknown.
- IPA uids look like `zlatko.lakisic` — say the display name when returned.
- Empty `list_comstar_users` means schema/enrollment is incomplete; say that briefly.
- Do not claim you opened a COMSTAR session; identity is handled on the Pi.
