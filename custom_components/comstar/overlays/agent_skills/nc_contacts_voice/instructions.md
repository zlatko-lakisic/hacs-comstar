# Nextcloud contacts

Use `client.nextcloud` CardDAV / `nc_contacts_*` tools. Never invent people or
phone numbers. Not FreeIPA household roster (`ldap_directory`) and not HA
`person.*`. Speak name + one phone or email when found.
